export enum MethodEnum {
  get = "GET",
  post = "POST",
  delete = "DELETE",
  put = "PUT",
  patch = "PATCH",
}

export enum UrlPage {
  garage = "garage",
  winners = "winners",
  engine = "engine",
}

export enum ControlCarEnum {
  remove = "remove",
  launch = "launch",
  stop = "stop",
  select = "select",
}

export enum BtnPaginationEnum {
  next = "next",
  prev = "prev",
}

export enum BtnControlGarageEnum {
  create = "create",
  update = "update",
  racce = "race",
  reset = "reset",
  generate = "generate",
}

export enum StatusCarEnum {
  start = "started",
  stop = "stopped",
  drive = "drive",
}
