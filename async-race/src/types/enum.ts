export enum MethodEnum {
  get = "GET",
  post = "POST",
  delete = "DELETE",
}

export enum UrlPage {
  garage = "garage",
  winners = "winners",
}

export enum ControlCarEnum {
  remove = "remove",
  launch = "launch",
  stop = "stop",
}
